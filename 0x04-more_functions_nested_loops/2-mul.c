#include "main.h"

/**
 * mul - multiplies two integes
 * @x: first number to multiply
 * @y: the second number to multiply
 * Return: the result of the multiplication
 */

int mul(int x, int y)

{
	return(x * y);
}
